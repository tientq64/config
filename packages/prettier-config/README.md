## @tientq64/prettier-config

> @tientq64's personal [prettier](https://prettier.io) configuration.

### Usage

**Install**:

```bash
pnpm add -D @tientq64/prettier-config prettier
```

**Edit `package.json`**:

```jsonc
{
    // ...
    "prettier": "@tientq64/prettier-config",
}
```
